题目：
给定一个二叉树和其中的一个结点，请找出中序遍历顺序的下一个结点并且返回。注意，树中的结点不仅包含左右子结点，同时包含指向父结点的指针。

就是求中序后继结点。

思路：
1、如果当前结点有右子结点，那么后继结点为当前结点的右子树中的最小值（也就是最左子结点）
2、如果没有右子结点：
2.1 如果当前结点有父结点：
2.1.1 如果当前结点是父结点的左子结点，后继结点为当前结点的父结点
2.1.2
如果当前结点是父结点的右子结点，后继结点为此结点的父结点的后继结点(不包括此结点为根的子树)
2.2 如果没有父结点，return None