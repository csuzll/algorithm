给定一个单链表的头节点，输出链表倒数第k个节点的值。（K从1开始取）

分析：
最简单的思路就按顺序访问链表节点，得到链表的长度x之后，再次从头节点出发，访问到第x-k+1个节点时，就是链表倒数第k个节点，但是这样的方法对前x-k+1个节点重复访问了两遍，效率较低。

在这个题目中，可以设置两个指针，一个为fast，一个为slow，fast先出发，在fast向前走了K-1步（此时fast指向正数第K个结点），slow从头节点出发，然后两个指针同时向后访问，当fast访问到最后一个节点时，slow访问到的就是倒数第k个节点。

注意判断链表长度小于K。